[pytest]
# Run pytest from /app/backend. Adding the backend root to sys.path so that
# `from app.middleware.org_context import OrgContext` and other top-level
# imports work without requiring `export PYTHONPATH=$(pwd)`.
pythonpath = .
testpaths = tests
asyncio_mode = auto
